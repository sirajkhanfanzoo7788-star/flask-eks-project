apiVersion: v1
kind: Service
metadata:
  name: flask-eks-projec-service

spec:
  type: LoadBalancer

  selector:
    app: flask-eks-projec

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000